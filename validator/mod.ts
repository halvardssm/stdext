/**
 * @stdext/validator - A simple validator implementing the Standard Schema specification
 * 
 * This package provides a type-safe validation library that implements the
 * Standard Schema V1 specification (https://standardschema.dev). It supports
 * primitives, nested objects, arrays, and custom validators.
 * 
 * @module
 */

// Re-export everything from the main validator module
export * from "./validator.ts";

// Re-export types
export * from "./types.ts";